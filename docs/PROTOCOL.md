# Laptop Link protocol v1

## BLE service

| Item | UUID | Properties |
|---|---|---|
| Service | `13EC0001-6C9A-4E1D-ABF5-4071BD729E3A` | Primary |
| Request | `13EC0002-6C9A-4E1D-ABF5-4071BD729E3A` | Write with response |
| Response | `13EC0003-6C9A-4E1D-ABF5-4071BD729E3A` | Notify |

Subscribe to Response before sending Hello. Use application authentication below; the protocol does not rely on the advertised name or OS Bluetooth pairing as identity.

Each application message is a four-byte unsigned **big-endian length**, followed by that many bytes of UTF-8 envelope JSON. Maximum envelope length: 262144 bytes. Split frames using the limits Core Bluetooth reports, never a hardcoded MTU. Client writes are sequential, each waiting for ATT completion. Server notifications pause when `updateValue` returns false and resume in `peripheralManagerIsReady(toUpdateSubscribers:)`.

Allow one request in flight per session, and wait for its full response before submitting the next. A write acknowledgement means the transport accepted that chunk, not that an operation completed. Notifications have no separate application chunk ACK/retransmission scheme. On a failed exchange, reconnect; retry a mutation with its original RPC identity. Invalid framing/authentication drops server session state; the central must disconnect and reconnect to subscribe afresh.

## Authentication and encryption

Protocol v1 retains its original transcript/AAD labels for compatibility with deployed peers. These wire constants are independent of the Laptop Link app and repository names.

All binary JSON fields use standard padded base64. Enrollment key `K` is 32 randomly generated bytes, transferred out of band. Nonces `C` and `S` are each 32 random bytes. Transcript `T` is the concatenation of ASCII `ble-connection/v1/`, `C`, and `S`.

1. Client → server: `{"type":"hello","nonce":"<C>"}`.
2. Server → client: `{"type":"challenge","nonce":"<S>","proof":"<HMAC-SHA256(K, T || ASCII(server))>"}`.
3. Client verifies the server proof and sends `{"type":"authenticate","proof":"<HMAC-SHA256(K, T || ASCII(client))>"}`.
4. Server verifies the client proof. Both derive two 32-byte keys with HKDF-SHA256: input key `K`, salt `T`, info ASCII `client-to-server` or `server-to-client`.
5. Server sends an encrypted `ready` byte string as its first data message. Subsequent plaintext messages are RPC JSON.

Encrypted envelope: `{"type":"data","sequence":0,"payload":"<ciphertext || 16-byte GCM tag>"}`.

AES-256-GCM nonce is four zero bytes followed by the sequence as an unsigned 64-bit big-endian integer. AAD is ASCII `ble-connection/v1/data`. Each direction starts at sequence zero and increments independently. The encrypted server `ready` consumes server sequence zero. Receivers require the exact next counter; invalid tags/counters close the session. Never reuse a session's keys/counters. A reconnect uses fresh random handshake nonces and new keys. No forward-secrecy claim is made: this is a shared-key enrollment design using CryptoKit primitives.

## RPC envelopes

Every request contains `version`, UUID `id`, `method`, and object `params`. `bootID` is optional only for `server.info`; other server methods require the current boot ID. The diagnostic client may look it up for read-only requests, but requires it explicitly for mutations.

```json
{"version":1,"id":"NEW-UUID","bootID":"CURRENT-BOOT-UUID","method":"fs.list","params":{"path":".","limit":100}}
```

Response contains `version: 1`, matching `id`, `bootID`, and either `result` or `error: {"code":"...","message":"..."}`. Optional fields are omitted, not guaranteed to appear as null. Results are bounded to 128 KiB before encryption; reduce page sizes if `response_too_large` is returned.

All operations except `server.info`, `fs.list/stat/read/search/hash`, `exec.poll/list`, and `upload.status` are treated as mutations. A duplicate mutation ID with identical parameters in the same boot returns its cached response, including cached failures. Different parameters produce `id_conflict`. Entries are retained for the server run, with an 8192-entry admission cap and no eviction. Read-only requests may execute again. Use globally unique UUIDs for new requests; retain the UUID and bootID on retries.

There is no exactly-once guarantee across server crashes. A previous bootID is rejected with `server_changed`. After a crash, inspect files/output to determine an operation's outcome before deliberately creating a new request. Cancellation cannot undo effects already performed.

## Methods

Paths are relative to the configured root or absolute paths inside it. Read/write paths follow symlinks after checking the resolved location. `fs.delete` and the source of `fs.move` operate on the final directory entry, so deleting/moving a symlink preserves its target. Intermediate symlinks are resolved. These checks are not a sandbox against hostile local races or arbitrary command execution.

| Method | Parameters | Result |
|---|---|---|
| `server.info` | `{}` | Name, root, boot ID, methods, basic limits |
| `fs.list` | `path="."`, `offset=0`, `limit=100` (1–500) | `entries`, nullable `next_offset`; at most 10000 directory entries scanned |
| `fs.stat` | `path` | Path, Foundation file-type string, size, numeric mode, Unix modified time |
| `fs.read` | `path`, `offset=0`, `length=32768` (1–65536) | Base64 `data`, offset, next offset, size, eof |
| `fs.hash` | `path` | `sha256` of regular-file bytes |
| `fs.search` | `query`, `path="."`, `content=false`, `limit=100` (1–500) | Matches, `truncated`, skipped count; literal case-sensitive search |
| `fs.write` | `path`, base64 `data`, optional `expected_sha256` or `overwrite=true` | Size and SHA-256; at most 65536 bytes |
| `fs.append` | `path`, base64 `data`, optional `expected_sha256` | New size; existing regular file required, at most 65536 bytes |
| `fs.patch` | `path`, `expected_sha256`, `edits:[{"old":"text","new":"text"}]` | Size and SHA-256; 1–100 sequential unique-match replacements, UTF-8 file ≤1 MiB |
| `fs.mkdir` | `path`, `parents=false` | `ok` |
| `fs.copy` | `source`, `destination` | `ok`; regular files only, destination must not exist |
| `fs.move` | `source`, `destination` | `ok`; destination must not exist |
| `fs.delete` | `path`, `recursive=false` | `ok`; root cannot be deleted |
| `upload.begin` | `path`, `size`, lowercase `sha256`, optional `expected_sha256` or `overwrite=true` | `upload_id`, `offset:0` |
| `upload.chunk` | `upload_id`, `offset`, base64 `data` | Upload ID, next offset, total size |
| `upload.status` | `upload_id` | Upload ID, next offset, total size |
| `upload.commit` | `upload_id` | Size and SHA-256 after verification/atomic replacement |
| `upload.abort` | `upload_id` | Last offset/size; upload removed |
| `exec.start` | See below | `job_id` |
| `exec.poll` | `job_id`, `stdout_offset=0`, `stderr_offset=0`, `max_bytes=16384` (1–32768 per stream) | Job state and output |
| `exec.cancel` | `job_id` | Poll-shaped response; termination may still be in progress |
| `exec.list` | `{}` | Job summaries with one output byte per stream |

Existing destinations require `expected_sha256` or explicit `overwrite:true` for write/upload. Hash mismatch produces `conflict`. Uploads accept nonempty chunks ≤65536 bytes at the exact next offset; duplicates should reuse the RPC ID. There are at most four active uploads, with one-hour inactivity expiry checked on upload operations. Transfers are only resumable during the same server run. Downloads use `fs.read` offsets and `fs.hash`; verify that the source did not change during transfer.

`fs.search` content matches include 1-based line numbers and the first 200 characters of the matching line. Results may omit files due to size, encoding, symlinks, permissions, or the time/entry limit; `skipped` and `truncated` expose this.

## Command jobs

`exec.start` accepts either:

```json
{"executable":"/usr/bin/git","args":["status","--short"],"cwd":"project","env":{"CUSTOM":"value"},"timeout_seconds":60}
```

or:

```json
{"shell":"printf 'hello'; printf 'warning' >&2; exit 7","cwd":".","timeout_seconds":30}
```

Executables must be absolute paths. `cwd` defaults to root. `timeout_seconds` defaults to 60 (or the configured maximum if smaller) and is between 1 and the configured maximum (default 3600). The server supplies a small default environment (`PATH`, `HOME`, `LANG`, `TMPDIR`); provided `env` entries override it. Shell expansion only happens in the explicit `shell` form, using `/bin/zsh -c`. Stdin is `/dev/null`.

`exec.poll` returns:

```json
{
  "job_id":"...","pid":123,"state":"exited","exit_code":7,"signal":null,
  "termination_requested":null,"started":1234567890.0,"finished":1234567891.0,
  "stdout":{"data":"aGVsbG8=","next_offset":5,"retained_bytes":5,"discarded_bytes":0,"error":null},
  "stderr":{"data":"d2FybmluZw==","next_offset":7,"retained_bytes":7,"discarded_bytes":0,"error":null}
}
```

States: `running`, `exited`, `timed_out`, `cancelled`. Timeout/cancel sends SIGTERM to the process group, escalating to SIGKILL after two seconds if the leader remains alive. The server kills remaining group members when the leader exits. `exit_code` is populated for normal exits; `signal` for signal termination. A timeout may end with a normal exit if the command handles SIGTERM.

After a terminal state, continue polling both streams until each `next_offset` equals `retained_bytes`. Decode base64, preserve binary bytes, and use an incremental UTF-8 decoder for text display. stdout and stderr have separate offsets; their global interleaving is not guaranteed. Truncation is explicit through `discarded_bytes`. Output retention is capped independently for each stream, while pipes continue draining to avoid deadlocks. Disk errors appear in the stream's `error` field.

BLE disconnect does not cancel a job; its execution timeout continues locally. A request/connection timeout at the client does not imply that the command stopped. Use `exec.cancel` or let the server execution timeout expire. Server shutdown kills active job groups; abrupt server crashes do not promise orphan cleanup or job recovery.
